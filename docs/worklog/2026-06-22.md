# 2026-06-22 Worklog

## Summary
- Phase 2 step3 channelConfirm 와이어링(adapter) — saga 첫 외부 호출 step. 외부 호출 Tx 분리 전역 컨벤션(얇은 비-Tx 리스너 + REQUIRES_NEW Tx 스텝) 확정.
- R4 보상 와이어링 — LIFO(외부 cancel → staging 취소), 트리거 2곳(validate REJECTED / channelConfirm FAILED).
- 토스 API 스펙 조사(channel-spec-analyst) — 주문조회/상태변경/seller-cancel 3 endpoint 공식 OpenAPI 확정.
- R6 토스 Port 정밀화 결정(modulith-architect + 사용자 4분기 합의) + 토스 구현체 구현(shared 타입 + adapter.ordr.infra.toss).
- hexa 리뷰 BLOCKER 0(SUGGESTION 6 중 3 적용). build+verify PASS, 단위 102 green. 2커밋 push.

---

## 작업 단위 (시간순)

### ① Phase 2 step3 channelConfirm 와이어링 (adapter)
- saga 첫 외부 호출 step. java-spring-expert 자문으로 **외부 호출 Tx 분리 전역 컨벤션** 확정: 얇은 비-Tx 리스너(`@TransactionalEventListener(AFTER_COMMIT)`) + `AdapterTxSteps`(mark / publishReply 각각 `@Transactional(REQUIRES_NEW)`), 외부 호출은 두 Tx 사이에서 Tx 밖 실행.
- `ChannelConfirmHandler` / `ChannelConfirmReplyHandler` / `ConfirmStrategyRegistry`(`Map<Channel,ConfirmStrategy>` 자동수집) + adapter 예외계층 `ChannelException`.
- `ValidateReplyHandler` 가 PASSED 시 step3 command 발행. step4 트리거(`ConfirmedOrderCommand`)까지 배선(소비자 미배선 — dangling).

### ② R4 보상 와이어링
- LIFO 순서(외부 cancel → staging 취소). 트리거 2곳(validate REJECTED / channelConfirm FAILED) → `OrderSagaCompensator`.
- saga: `ChannelConfirmCompensatedHandler`(OK → staging 취소 진행 / MANUAL_REQUIRED → `PENDING_MANUAL_CANCEL` terminal / FAILED → COMPENSATING 유지) + `UnconfirmedOrderCompensatedHandler`(→ COMPENSATED).
- adapter: `ChannelConfirmCompensationHandler`(외부 cancel, mark-after-success — cancel 멱등이라 실패 시 재시도 보존) + `UnconfirmedOrderCompensationHandler`(staging CANCELED, 순수 DB).
- `CompensationPortRegistry`(미등록 채널 → `Optional.empty()` → MANUAL_REQUIRED), `ManualCancelRequiredException`.
- `CompensateUnconfirmedOrderRequested` 를 stagingId → externalOrderProductId 자연키로 정렬.

### ③ 토스 API 스펙 조사 (channel-spec-analyst)
- 공식 OpenAPI 로 3 endpoint 확정:
  - 주문조회 `GET /orders/v2`(cursor 페이지네이션, 31일 조회 범위, 단건 GET 부재).
  - 상태변경 `PUT /orders/products/status`(배열 입력 + 부분성공 응답, PAID → PREPARING_PRODUCT).
  - seller-cancel `POST /order-products/{id}/seller-cancel`(`deliveryPenaltyCharger` USER|MERCHANT 필수).
- RL Key 헤더 · TOO_MANY_REQUEST(HTTP 200 함정) · enum 3집합(읽기 19 / 쓰기 4 / 필터 6) 정리.
- MSA 6항목 대비 델타 기록.

### ④ R6 토스 Port 정밀화 결정 (modulith-architect + 사용자 4분기 합의)
- `OrderQueryPort` 신설(단건 GET 부재 → 사전 GET 가드는 목록 조회로 충당).
- shared 추상 `ExternalOrderStatus` 도입(채널 raw status → 추상 매핑).
- `CancelReason` + `DeliveryPenaltyCharger` enum 승격(shared).
- 토스 구현체만 Phase 2 진입(네이버 · 쿠팡은 R5b D-5 격리 유지).

### ⑤ 토스 구현체 구현 (단위 ① shared + ② adapter.toss)
- 단위 ① shared 타입: `ExternalOrderStatus` / `DeliveryPenaltyCharger` / `CancelReason` + `CompensationPort.cancel` 시그니처 → `CancelReason` 수용 + `OrderQueryPort` + `CompensateChannelConfirmRequested` 에 charger 추가.
- 단위 ② `adapter.ordr.infra.toss`:
  - `TossProperties` / `TossConfig`.
  - `TossTokenManager`(OAuth2 client credentials, 토큰 캐시 + 401 시 갱신).
  - `TossApiClient`(RestClient seam, resultType / TOO_MANY_REQUEST / 401 처리).
  - `TossOrderStatus`(19 raw → 추상 매핑).
  - `TossOrderQueryAdapter` / `TossConfirmStrategy`(사전 GET 가드 + 부분성공 처리) / `TossCompensationPort`(GET 가드 + transient ↔ manualRequired 분류 + reason 절단).
- 단위 테스트 다수.

### ⑥ hexa-clean-reviewer 리뷰
- BLOCKER 0. SUGGESTION 6 중 3 적용:
  - `verifyOk` null envelope 가드.
  - `changeProductStatus` enum 타입화.
  - reason / detailReason 100 / 250 절단.
- 3 이연 · 미적용: Clock 주입(batch / core 동시적용 전제), charger String(infra 경계), `transient_` 네이밍.

---

## 합의된 정책 (이번 세션 신규)

| 정책 | 결정 |
|---|---|
| 외부 호출 Tx 분리 | 얇은 비-Tx `@TransactionalEventListener(AFTER_COMMIT)` 리스너 + `AdapterTxSteps`(mark/publishReply `@Transactional(REQUIRES_NEW)`). 외부 호출은 두 Tx 사이 Tx 밖 |
| 보상 LIFO | 외부 cancel → staging 취소 순. 트리거 2곳(validate REJECTED / channelConfirm FAILED) → `OrderSagaCompensator` |
| 보상 멱등 | 외부 cancel 은 mark-after-success(cancel 멱등이라 실패 시 재시도 보존). staging 취소는 순수 DB |
| 미등록 채널 보상 | `CompensationPortRegistry` Optional.empty → MANUAL_REQUIRED → `PENDING_MANUAL_CANCEL` terminal |
| 토스 Port (R6) | `OrderQueryPort` 신설(단건 GET 부재) / shared 추상 `ExternalOrderStatus` / `CancelReason`+`DeliveryPenaltyCharger` enum 승격 / 토스 구현만 Phase 2 진입 |
| 토스 호출 | OAuth2 client credentials 토큰 캐시 + 401 갱신, TOO_MANY_REQUEST(HTTP 200) 처리, RestClient seam |

---

## git
- 2커밋 push: `32e0ecd`(Phase 2 A1 SAGA 와이어링 + R6 토스 Adapter 구현, 84 files) / `046ce1a`(문서 — worklog 06-18·06-19 / architecture / getting-started), `feature/sketch` → origin.

---

## 검증 상태
- build + `ApplicationModules.verify()` PASS.
- 단위 102 green. 6 실패는 Docker 미가동 Testcontainers 슬라이스(코드 무관).

---

## 다음 진입점 후보

| 후보 | 설명 |
|---|---|
| @ApplicationModuleTest / @SpringBootTest 통합 테스트 | externalization off + `@MockitoBean` 외부 채널 스텁 + Testcontainers 4 schema. 비동기 listener · Tx · outbox dispatch 실배선 검증 (착수 예정) |
| Phase 2 step4 confirmedOrder Pivot (core) | `ConfirmedOrderCommand` 소비자 미배선(dangling). LogisticsGateway dispatch + Order INSERT + reconciliation(R4 N=3) |
| 네이버 · 쿠팡 Port 구현 | R5b D-5 격리 해제 — 쿠팡 403 본문 재위임 / 네이버 필드명 · path 확정 후 |
