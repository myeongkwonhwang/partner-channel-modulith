# 2026-06-18 Worklog

## Summary
- 빌드 스택 업그레이드: Java 21→25, Spring Boot 3.4.5→4.0.7, Spring Modulith 1.3.3→2.0.7.
- A1(주문) SAGA 설계 재정의 R2~R5b 완료 (외부 상태 전이 / 멱등성 / 보상 / 운영 / 다채널).
- validate 거절 처리 정밀화: 토스 패널티 조사 → seller-cancel + 3중 안전망(알림·게이팅).
- Phase 1 코드 착수: shared 모듈(22 파일) + core 모듈(17 파일) 골격, build+verify PASS, hexa BLOCKER 0.
- 커밋·push 3건(빌드스택 / shared / core), `feature/sketch`.

---

## 작업 단위 (시간순)

### ① 빌드 스택 업그레이드
- `build.gradle`: Java toolchain 21→25, `org.springframework.boot` 3.4.5→4.0.7, Modulith BOM 1.3.3→2.0.7, dependency-management 1.1.6→1.1.7, ArchUnit 1.3.0→1.4.2.
- `jackson-datatype-jsr310` 의존 제거 (Boot 4 / Jackson 3 코어에 JSR-310 내장).
- 호환 매트릭스 Maven Central 확인: Boot 4.0 ↔ Modulith 2.0 라인 묶임, 로컬 JDK 25.0.3 존재.
- `./gradlew clean build` PASS. 커밋 `664f0ff`.

### ② R2 — 외부 상태 전이
- 외부 PREPARING_PRODUCT 전이를 step1 에서 분리해 validate 통과 후 별도 step(channelConfirm)으로(C′). MSA 와 분기.
- saga 시작 트리거 = in-VM `@ApplicationModuleListener`(외부 Kafka self-consume 회피).
- command/reply event = `shared/event` + `@NamedInterface`.
- 처리량 스케일은 물리 분리 없이 split-ready Modulith 유지 결정(java-spring-expert 자문: in-VM 채택, 다중 인스턴스 시 내부 전용 토픽 하이브리드).

### ③ R3 — 멱등성
- `processed_event` 각 모듈 자기 schema 로컬, 복합 PK `(consumer_name, event_id)`.
- 발행측 내장 outbox(`event_publication`, 유실 방지) ↔ 수신측 dedup(중복 흡수) 책임 분리.
- 가드 = native `INSERT ON CONFLICT DO NOTHING` + 영향행수, 비즈니스 DB 쓰기와 같은 로컬 Tx.
- 멱등 scope = saga 진입 + step 개별(validate 생략, reply 가드 포함).

### ④ R4 — 보상 흐름
- 역순 LIFO, 외부→내부 순서. step3 분리로 MSA 단일 step1 보상이 2개(외부 seller-cancel + staging 취소)로 분리.
- 외부 cancel = `CompensationPort` 추상 + `PENDING_MANUAL_CANCEL` fallback(토스 seller-cancel 가용성 FAQ 모순 흡수).
- 보상 멱등 = R3 processed_event 재사용(`consumer_name="compensate:{step}"`). 사전 GET 가드 직교 이중 방어.
- Pivot(step4) 부분 실패 = reconciliation N=3.

### ⑤ R5a — 운영 골격 (채널 무관)
- ShedLock polling / saga TIMEOUT·stuck scanner(RUNNING 30m·COMPENSATING 1h) / RECEPTION_DELAY `@ConfigurationProperties` / skip 메트릭(Micrometer) / processed_event cleanup(30d > republish 7d) / PENDING_MANUAL_CANCEL 운영 + seller-cancel 재시도 N=3.
- DLQ Modulith 분기: MSA 4 토픽 불승계 — 내부=dead publication / 외부 @Externalized=Kafka DLQ / 보상 영구실패=PENDING_MANUAL_CANCEL.

### ⑥ R5b — 다채널 전략 + API 규격 조사
- channel-spec-analyst 조사: 네이버·쿠팡 분·시 polling(옵션 D) + 발주확인 전용 API 존재. 유튜브쇼핑 네이티브 주문 API 부재.
- 3 Strategy Port(PollingStrategy=batch / ConfirmStrategy=adapter / CompensationPort 채널 확장). `Map<Channel,Strategy>` Registry 디스패치.
- Channel enum 에 NAVER·COUPANG 추가. 유튜브 보류(연동 플랫폼 합의 시 편입). 미확정 spec 은 Port 격리 후 Phase 2 이연.

### ⑦ validate 거절 처리 정밀화
- 토스 패널티 공식 문서 조사: 판매자 임의 취소 4점(정당 사유 면제 X), PAID 방치 자동취소 규정 없음(발송기한 미준수 1점 + 미노출 → 장기 미발송 10점). 방치(11점) > seller-cancel(4점), 무패널티 출구 없음.
- R4 보강: validate 거절 보상 = 외부 seller-cancel + staging 취소(2액션). `deliveryPenaltyCharger=MERCHANT`. R2 Q1 전제 정정(외부 미통보 ≠ 외부 부수효과 없음, PAID 는 외부 사실).
- 3중 안전망 추가: ① 알림(log.info + `validate.rejected{channel,reason}` 메트릭) ② 단건 seller-cancel ③ 게이팅(거절 급증 시 자동취소 보류→PENDING_MANUAL, 서킷브레이커). 임계치·윈도우는 R5a ⑪ 이연.

### ⑧ Phase 1 shared 모듈 골격
- 22 파일: `domain/Channel`(3채널), `exception/PartnerChannelException`(abstract base), `policy/ReceptionDelay`, `idempotency/`(ProcessedEvent record + Repository port `markIfFirst` + ProcessedEventId IdClass + ProcessedEventJpaEntity @MappedSuperclass + EventKey), `event/ordr/`(OrderReceivedEvent @Externalized + step command/reply 8 + 보상 4 + package-info).
- 빌드 실패 1건(ChannelConfirmReply.failed 인자 수) 수정 후 build+verify PASS. hexa BLOCKER 0.
- 커밋 `91a337d`.

### ⑨ Phase 1 core 모듈 골격
- 17 파일: `core.ordr` nested `@ApplicationModule`("Core :: Order" 별도 모듈 인식 확인) + `core.claim`/`core.invc` 빈 슬롯.
- domain(Order record — sagaId 보유, OrderRepository port) / application(Validator+Verdict, LogisticsGateway 포트+Shipment 3종) / infra(OrderJpaEntity @Table(schema=core_schema), CoreProcessedEventJpaEntity final extends shared @MappedSuperclass + native ON CONFLICT, MockLogisticsGateway).
- Flyway: core_schema.processed_event 를 R3 복합키로 정정(V1 직접 수정, 미배포 scaffold).
- 빌드 실패 1건(Verdict record 컴포넌트명 passed ↔ 정적팩토리 충돌 → sellable 로 변경) 수정 후 build+verify PASS. hexa BLOCKER 0.
- 커밋 `7669e0d`.

### ⑩ 문서 작성 (untracked)
- `docs/worklog/2026-06-18.md`, `docs/architecture/a1-order-saga-flow.md` 작성(코드 흐름 + 보상/3중 안전망 반영). git stage 제외 요청.

---

## 합의된 정책 (이번 세션 신규)

| 정책 | 결정 |
|---|---|
| 빌드 스택 | Java 25 / Boot 4.0.7 / Modulith 2.0.7 / ArchUnit 1.4.2, jsr310 제거 |
| core 도메인 분할 | nested `@ApplicationModule`(core.ordr/claim/invc), core 직속 패키지엔 클래스 금지 |
| 헥사고날 레이어 | domain / application / infra 3계층 |
| schema 라우팅 | 단일 DataSource·단일 EMF + `@Table(schema)` 명시(multi-EMF 기각) |
| Phase 1 진행 | 모듈별 순차(shared→core→batch→adapter→saga), 각 단계 build+hexa 검토 |
| validate 거절 | seller-cancel(MERCHANT) + 3중 안전망(알림·게이팅), 방치 금지 |

---

## git
- 커밋·push 3건: `664f0ff`(빌드스택) / `91a337d`(shared) / `7669e0d`(core), `feature/sketch` → origin 동기화.
- `docs/` untracked(요청대로 stage 제외).

---

## 다음 진입점 후보

| 후보 | 설명 |
|---|---|
| Phase 1 batch 모듈 | batch.ordr nested module + PollingStrategy 포트 + OrderReceptionEligibilityFilter(30분 가드) + cursor JPA + channel_schema 멱등 |
| (이후) adapter → saga | ConfirmStrategy/CompensationPort/staging → SagaState + 오케스트레이션 골격 |
| 쿠팡 403 본문 재조사 | Phase 2 진입 전 channel-spec-analyst 재위임(body schema/RL/취소 재호출) |
| 유튜브 편입 | 연동 커머스 플랫폼 확정 합의 시 |
