# 2026-06-19 Worklog

## Summary
- Phase 1 order vertical 마지막 모듈(adapter, saga) 골격 + 테스트 완료 → 5모듈(shared/core/batch/adapter/saga) 골격 완성.
- 냉정 리뷰 후 Phase 2 A1 SAGA Application Events 와이어링 착수: sagaStart → step1 unconfirmedOrder → step2 validate 완료.
- 멱등 주입 모호성 해소: shared ProcessedEventRepository → 모듈별 전용 Port(R3/D-3 부분 조정).
- 전체 78 테스트 green, build + ApplicationModules.verify() PASS.
- adapter 2커밋 push, 이후(saga·refactor·Phase 2)는 staged 미커밋.

---

## 작업 단위 (시간순)

### ① adapter.ordr 모듈 골격
- `adapter.ordr` nested `@ApplicationModule` + domain(`StagingOrder` 불변 newRecord/canceled, `StagingOrderRepository`) + application(`ConfirmStrategy`/`CompensationPort` Port 골격, 구현 Phase 2 이연) + infra(persistence `staging_order` @Table(channel_schema), raw=jsonb `@JdbcTypeCode(JSON)` / idempotency `AdapterProcessedEvent` 트리오).
- 설계: batch 와 같은 물리 테이블 `channel_schema.processed_event` 를 모듈별 별도 @Entity(`AdapterProcessedEventJpaEntity` ≠ batch `ChannelProcessedEventJpaEntity`)로 매핑, consumer_name 으로 행 분리.
- 11파일. 커밋 `aa6c1a4`, push.

### ② adapter.ordr 테스트 19
- domain 8(NPE 가드·canceled 전이/멱등 NoOp) + persistence 7(id 채번·schema 라우팅·jsonb 의미동등 `raw=?::jsonb`·UNIQUE·CANCELED 전이) + idempotency 4(첫 true/중복 false·consumer 독립·20스레드 race).
- 커밋 `bbc6d85`, push.

### ③ saga.ordr 모듈 골격 + 테스트
- `saga.ordr` nested module + domain(`SagaState` 불변 start/advanceTo/withStatus/incrementReconciliationAttempts, `SagaStateRepository`) + infra(persistence `saga_state` @Table(saga_schema), payload=jsonb nullable / idempotency `SagaProcessedEvent` 트리오).
- Flyway: saga V1 baseline 에 `processed_event` 부재 → core/channel 과 동일 복합 PK 구조로 V1 직접 추가(미배포 scaffold).
- step/status 는 String(state machine·상수는 Phase 2 이연).
- 9파일 + 테스트 4파일(domain 11·persistence 7·idempotency 4 = 22). build+verify+64테스트 green. git stage.

### ④ 냉정 코드 리뷰
- hexa-clean-reviewer 에이전트 2회 529 과부하 → 직접 리뷰 수행.
- BLOCKER 0. 최우선 SUGGESTION = S1(merge UPDATE 테스트가 Port 왕복으로 L1 캐시를 읽어 DB 실반영 미검증). 그 외 status enum(S2)·co-owned table consumer_name 미강제(S4) 등.
- 총평: 골격이 깨끗한 건 어려운 부분(Tx 원자성·외부 호출·예외계층)을 Phase 2 로 미뤘기 때문.

### ⑤ S1 — merge UPDATE 테스트 사각지대 수정
- `StagingOrderRepositoryAdapterTest`(CANCELED)·`SagaStateRepositoryAdapterTest`(advanceTo)에서 `em.flush()` 로 지연 UPDATE 를 DB 강제 후 native read 로 검증(L1 우회).

### ⑥ 멱등 주입 모호성 해소 — 모듈별 전용 Port
- shared 단일 `ProcessedEventRepository` 구현 4개라 타입 주입 시 `NoUniqueBeanDefinitionException`(Spring DI 는 Modulith 모듈 스코프 아님). 기록됐던 "모듈-로컬 단일 후보" 전제 오류.
- shared Port 삭제 → `SagaIdempotencyGuard`/`CoreIdempotencyGuard`/`BatchIdempotencyGuard`/`AdapterIdempotencyGuard`(각 `.ordr.application`). 4 adapter 가 각자 Port 구현. 물리 구조(`ProcessedEventJpaEntity` @MappedSuperclass + 모듈별 @Entity) 유지.

### ⑦ Phase 2 sagaStart
- `OrderSagaStarter`(@ApplicationModuleListener on `OrderReceivedEvent`): markIfFirst(sagaStart, correlationKey) → `SagaState.start(A1_ORDER/UNCONFIRMED_ORDER/RUNNING)` save → `UnconfirmedOrderCommand` 발행. 멱등 마킹+save 같은 saga_schema Tx, 발행은 내장 outbox 경유.
- `A1OrderSaga` 상수(TYPE/step), `EventKey.of(String,String)` 오버로드. 단위 테스트 2(Port mock).

### ⑧ Phase 2 step1 unconfirmedOrder
- adapter `UnconfirmedOrderHandler`: markIfFirst(unconfirmedOrder) + `StagingOrder.newRecord`(ACTIVE) save 같은 channel_schema Tx → `UnconfirmedOrderReply.ok`.
- saga `UnconfirmedReplyHandler`: OK→findBySagaId(없으면 `SagaStateNotFoundException`)+markIfFirst(unconfirmedOrderReply, race-safe)+advanceTo(VALIDATE) / FAILED→로그.
- 계약 조정: `UnconfirmedOrderCommand` 에 `externalOrderProductId` 추가(staging UNIQUE·멱등 키, polling 이 이미 추출, raw 재파싱은 미확정 spec 추정금지). sagaStart 도 전달하도록 수정.
- 예외계층 도입(첫 thrower): `SagaException`(abstract) + `SagaStateNotFoundException`(final, 메시지에 sagaId).
- 단위 테스트 6.

### ⑨ Phase 2 step2 validate
- saga `UnconfirmedReplyHandler` advance 후 `ValidateCommand` 발행(channel=`EventKey.channelCodeOf(correlationKey)`, raw=`SagaState.payload`).
- core `ValidateHandler`(read-only·멱등 생략·R3): `Validator` → `ValidateReply`(PASSED/REJECTED).
- saga `ValidateReplyHandler`: PASSED→markIfFirst+advanceTo(CHANNEL_CONFIRM) / REJECTED→로그(R4 미소비 유지).
- 단위 테스트 6. build+verify+78테스트 green.

---

## 합의된 정책 (이번 세션 신규)

| 정책 | 결정 |
|---|---|
| 멱등 주입 | 모듈별 전용 Port(Saga/Core/Batch/AdapterIdempotencyGuard). shared ProcessedEventRepository 삭제(R3/D-3 부분 조정) |
| SAGA 오케 패턴 | reply 핸들러가 advance + 다음 step command 발행. 다음 step command 발행은 그 step 와이어링 시 추가(미소비 dangling 회피) |
| 이벤트 발행 | `ApplicationEventPublisher` 직접(Modulith 관용, publish 마다 포트 래핑 안 함) |
| reply 멱등 | processed_event `markIfFirst`(원자) — 상태머신 가드 단독은 load-advance-save lost-update race |
| 예외 계층 | `SagaException`(abstract, PartnerChannelException sub) + final 구체. thrower 와 함께 도입 |
| command 데이터 | staging 식별자는 command 운반(raw 재파싱 금지 — 미확정 채널 spec 추정금지) |
| merge UPDATE 검증 | Port 왕복(L1) 금지 → `em.flush()` 후 native read |
| 작업 마무리 | 작업 단위마다 테스트 작성 + git stage 기본 절차(commit/push 는 명시 지시 시) |

---

## git
- 커밋·push 2건: `aa6c1a4`(adapter 골격) / `bbc6d85`(adapter 테스트), `feature/sketch` → origin.
- 이후 전부(saga 모듈, 멱등 Port 리팩터, S1, Phase 2 sagaStart/step1/step2) **staged·커밋 미실행**(38 files staged). shared `ProcessedEventRepository` 삭제 포함.
- `docs/` untracked.

---

## 다음 진입점 후보

| 후보 | 설명 |
|---|---|
| Phase 2 step3 channelConfirm (adapter) | ChannelConfirmCommand → ConfirmStrategy 외부 호출(Tx 밖!) → reply → advance(CONFIRMED_ORDER). ConfirmStrategy 채널 구현 + Map<Channel,Strategy> Registry(R5b D-2), 토스 PUT status spec(channel-spec-analyst 선행 가능), 외부+DB Tx 분리가 핵심 |
| Phase 2 step4 confirmedOrder Pivot (core) | LogisticsGateway dispatch + Order INSERT + reconciliation(R4 N=3) |
| 보상 (R4) | validate REJECTED / step3 실패 → seller-cancel + staging 취소. 현재 미소비 로그만 |
| @ApplicationModuleTest 통합 | listener 비동기·Tx·outbox(event_publication) dispatch 실배선 검증 |
| externalOrderProductId 정규 식별자 전략 | PollingStrategy 구현 시 결정. 채널별 식별자·granularity(channel-spec-analyst 선행) |
